import 'package:flutter/material.dart';

class SelectOrder extends StatelessWidget {
  const SelectOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('SelectOrder');
  }
}
